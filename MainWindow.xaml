<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="$safeprojectname$.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:$safeprojectname$"
    xmlns:models="using:$safeprojectname$.ViewModels"
    Title="$safeprojectname$">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" Loaded="RootGrid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="titleBar"
            Title="$safeprojectname$"
            Grid.Row="0"
            Height="48"
            x:FieldModifier="public"
            BackRequested="titleBar_BackRequested"
            IsBackButtonVisible="False"
            IsPaneToggleButtonVisible="True"
            PaneToggleRequested="titleBar_PaneToggleRequested">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="ms-appx:///Assets/MainWindow.ico" />
            </TitleBar.IconSource>
            <TitleBar.RightHeader>
                <StackPanel
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="16">
                    <Button
                        Width="30"
                        Height="30"
                        Padding="0"
                        Background="DarkGray"
                        Click="BtnOpenAI_Click"
                        CornerRadius="15"
                        Foreground="White">
                        <Button.Resources>
                            <!--  ホバー時の色調整（任意）  -->
                            <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="Gray" />
                            <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="White" />
                        </Button.Resources>
                        <FontIcon FontSize="18" Glyph="&#xE897;" />
                    </Button>
                </StackPanel>
            </TitleBar.RightHeader>
        </TitleBar>

        <NavigationView
            x:Name="NavView"
            Grid.Row="1"
            x:FieldModifier="public"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="True"
            ItemInvoked="NavView_ItemInvoked"
            PaneDisplayMode="Left">
            <NavigationView.MenuItems>
                <NavigationViewItem
                    Content="ホーム"
                    Icon="Home"
                    Tag="HomePage" />
            </NavigationView.MenuItems>

            <!--  Frame をここに配置  -->
            <Frame x:Name="ContentFrame" />
        </NavigationView>

        <!--  トースト通知レイヤー  -->
        <ItemsControl
            Grid.Row="1"
            Margin="20"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Canvas.ZIndex="1000"
            ItemsSource="{x:Bind ViewModel.ToastNotifications, Mode=OneWay}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="models:ToastNotificationModel">
                    <InfoBar
                        Margin="0,0,0,10"
                        IsOpen="True"
                        Message="{x:Bind Message}"
                        Severity="{x:Bind Severity}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>